# Copy to .env for local Studio dev (`npm run dev` inside /studio).
# Same project ID/dataset as the storefront's NEXT_PUBLIC_SANITY_PROJECT_ID
# and NEXT_PUBLIC_SANITY_DATASET.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
